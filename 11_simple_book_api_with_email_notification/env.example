# Database Configuration
MONGODB_URI=mongodb://localhost:27017/bookapi

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here

# Email Configuration
# SMTP Server Settings (Required)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Email Sender Configuration
SMTP_FROM_EMAIL=your-email@gmail.com

# Default recipient email (fallback if user email is not available)
DEFAULT_RECIPIENT_EMAIL=admin@yourcompany.com

# Server Configuration
PORT=3000

# Email Provider Examples:
# 
# Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# Note: Use App Password, not regular password
#
# Outlook:
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false
#
# Mailtrap (for testing):
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_SECURE=false
#
# Ethereal (for testing):
# SMTP_HOST=smtp.ethereal.email
# SMTP_PORT=587
# SMTP_SECURE=false 